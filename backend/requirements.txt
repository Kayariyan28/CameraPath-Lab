# CameraPath Lab — backend runtime deps (macOS / Apple Silicon, no CUDA)

# --- web layer ---
fastapi==0.121.2
uvicorn[standard]==0.41.0
pydantic==2.13.0
python-multipart==0.0.20

# --- numerics ---
numpy>=2.1,<3
scipy>=1.14,<2

# --- computer vision ---
# Pinned to OpenCV 4.x: 5.x is a major release with API removals we have not
# validated against (SIFT/LK/RANSAC call sites in tracking/ and solvers/).
opencv-contrib-python>=4.10,<5

# --- classical 3D reconstruction (Phase 2) ---
pycolmap>=3.11,<5
pydantic-settings>=2.6

# --- agent integration (MCP server + the HttpRunner's delegated mode) ---
# Pinned to the 1.x line: mcp 2.x removed FastMCP (renamed MCPServer) and pulls
# in httpx2/httpcore2/opentelemetry. 1.30.0 is the last 1.x and resolves against
# our pydantic/starlette/uvicorn/anyio pins with zero changes to them.
mcp==1.30.0
# Runtime, not just tests: the delegated runner talks to the local backend over
# HTTP. Previously only in requirements-dev.txt.
httpx>=0.27,<1
