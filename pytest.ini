[pytest]
testpaths = tests
pythonpath = . backend
addopts = -ra --strict-markers
markers =
    slow: takes more than a few seconds
    blender: requires a working Blender install
    synthetic: runs against Blender-rendered ground truth
filterwarnings =
    error::DeprecationWarning:app.*
